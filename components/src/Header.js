import React, {Component} from 'react';
import logo from './logo.svg';

export default class Headers extends Component{
  render(){
    return (
      <header className="App-header"> <img className="App-logo" alt="alternate-text" src={logo}/> < h1> Welcome to React-JS. </h1> </header>
    );
  }
}
